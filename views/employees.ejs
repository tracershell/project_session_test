<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <title>Dashboard</title>
  <style>
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }
    body {
      font-family: 'Segoe UI', sans-serif;
      display: flex;
      min-height: 100vh;
      background-color: #f9f9f9;
    }
    .sidebar {
      width: 220px;
      background-color: #343a40;
      color: white;
      padding-top: 40px;
      flex-shrink: 0;
    }
    .sidebar h2 {
      text-align: center;
      margin-bottom: 20px;
      font-size: 22px;
    }
    .menu {
      list-style: none;
      padding: 0;
    }
    .menu li {
      padding: 15px 20px;
      cursor: pointer;
    }
    .menu li:hover {
      background-color: #495057;
    }
    .menu li.active {
      background-color: #007bff;
    }

    .main {
      flex-grow: 1;
      padding: 40px;
      position: relative;
    }

    /* ✅ 상단 우측 사용자 정보 */
    .user-info {
      position: absolute;
      top: 20px;
      right: 40px;
      color: #555;
      font-size: 14px;
    }
    .user-info span {
      font-weight: bold;
      color: #007bff;
    }
  </style>
</head>
<body>

  <div class="sidebar">
    <h2>📊 DASHBOARD</h2>
    <ul class="menu" id="menu">
      <li data-href="/general">general</li>
      <li class="active" data-href="/employees">employees</li>
      <li data-href="/vernon">vernon</li>
      <li data-href="/la">la</li>
      <li data-href="/buenapark">buenapark</li>
    </ul>
  </div>

  <div class="main">

    <!-- ✅ 세션 사용자 정보 표시 -->
    <div class="user-info">
      👤 <span><%= user.username %></span> (<%= user.role %>) | <a href="/logout">로그아웃</a>
    </div>

    <h1>Welcome to your dashboard!</h1>
    <p>이곳은 로그인 후 사용자에게 보여지는 대시보드입니다.</p>
    <p>왼쪽 메뉴를 클릭하여 지역별 데이터를 조회하세요.</p>
  </div>

    <!-- ✅ 메뉴 클릭 시 .active 처리 스크립트 -->
    <script>
  const menuItems = document.querySelectorAll('#menu li');

menuItems.forEach(item => {
  item.addEventListener('click', () => {
    // ⚡ active 상태 변경
    menuItems.forEach(i => i.classList.remove('active'));
    item.classList.add('active');

    // 🌐 해당 경로로 이동
    const target = item.getAttribute('data-href');
    if (target) {
      window.location.href = target;
    }
  });
});
    </script>

</body>
</html>
